const{json:a}=require('express');function b(){return`
    <section aria-labelledby="home-titulo">
      <h1 id="home-titulo">Bem-vindo à ONG</h1>
      <p>Conheça nossos projetos e participe!</p>
    </section>
  `}function A(){return`
    <section aria-labelledby="cadastro-titulo">
      <h2 id="cadastro-titulo">Cadastro</h2>
      <form id="form-cadastro" aria-label="Formulário de cadastro">
        <label for="nome">Nome</label>
        <input type="text" name="nome" id="nome" required />

        <label for="email">Email</label>
        <input type="email" name="email" id="email" required />

        <button type="submit">Enviar</button>
      </form>
      <div id="feedback" aria-live="polite"></div>
    </section>
  `}function d(){return`
    <section aria-labelledby="projetos-titulo">
      <h2 id="projetos-titulo">Projetos</h2>
      <p>Veja os cadastros realizados:</p>
      ${f()}
    </section>
  `}function _(B){let C=document.getElementById('app');switch(B) {case 'home':C.innerHTML=b();break;case 'cadastro':C.innerHTML=A();break;case 'projetos':C.innerHTML=d();break}}doxument.addEventListener('submit',function(e){if(e.target.id==='form-cadastro'){e.preventDefault();let _a=e.target.nome.value.trim(),_b=e.target.email.value.trim(),_c=document.getElementById('feedback');if(_a===''||!_b.includes('@'))_c.innerHTML='<p class="error">Preencha os dados corretamente.</p>';else{let D={nome:_a,email:_b},_B=a.parse(localStorage.getItem('cadastros'))||[];_B.push(D);localStorage.setItem('cadastros',JSON.stringify(D));_c.innerHTML='<p class="success">Cadastro salvo com sucesso!</p>';e.target.reset()}}});function f(){let _A=JSON.parse(localStorage.getItem('cadastros'))||[];return`
    <ul aria-label="Lista de cadastros realizados">
      ${_A.map(c=>`<li>${c.nome} - ${c.email}</li>`).join('')}
    </ul>
  `}function g(){document.body.classList.toggle('tema-escuro')}_('home');
